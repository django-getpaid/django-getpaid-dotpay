============
Installation
============

At the command line::

    $ easy_install getpaid-dotpay

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv getpaid-dotpay
    $ pip install getpaid-dotpay
